# Sophia-Local 优化计划 v5

> 基于原版官方更新历史（https://www.sophia.app/zh/changelog，v1.0.1 → v4.5.0）逐条核对，
> 排除宠物养成/孵化、社区论坛、云应用与跨设备、书店与兴趣推荐、语音服务选型、课堂背景、
> 能量条、主题外观、推广返利等产品化内容后的学习相关差距。
> v1–v4 计划（plan.md / plan-v2/v3/v4）全部完成，以下为补齐项。
> 进度标记：[ ] 待办  [~] 进行中  [x] 已完成

---

## A. 课堂质量（核心）

### A1. 跨章节跳转更克制
- **现状**：A1 检索让伙伴能在全书范围内找相关段落（4.0.0），但没有"默认按章节顺序推进"的约束，
  伙伴可能在本章未讲完时主动建议跳到后面章节学某块知识。
- **参考**：1.0.9「AI 默认按章节顺序推进，跨章只在明确要求时才执行；你只是提及后续章节的概念，不会被牵走」。
- **实现思路**：`rules.ts` 新增「章节顺序推进」规则段：默认沿当前章节顺序推进；只有学习者明确要求
  才跳转/跨章找内容；学习者只是提及后续章节概念时不要被牵走。与 A1 检索共存——检索用于把当前概念的
  跨章段落带回课堂，而非让伙伴主动提议换章。
- **涉及文件**：`rules.ts`、`prompt-builder.ts`
- **状态**: [x]

### A2. 跨课时间感 + 开场不接戏
- **现状**：`handoff_meta.json` 已存 `savedAt / prevConvId / companionName / companionSlot / endingPage`，
  但只用了 `prevConvId` 定位 handoff tail，时间/位置信息没有注入 prompt。伙伴可能把上节课说成"今天/刚才"。
- **参考**：1.0.8「同一教材开新一节课时，伙伴会把上节课的内容定位在"上次"而不是"刚才/今天"」；
  2.0.0「新一节课的开场不会再"接戏"上节课结尾的场景」。
- **实现思路**：
  1. `chat-prompt.ts` 的 `loadHandoffTail` 一并读出 `handoff_meta.json` 的 `savedAt`、`endingPage`，
     返回 `{ tail, meta }` 结构（含上次结束时间、上次读到页码）。
  2. `prompt-builder.ts` 新增「上次课堂时间线」段：用相对时间措辞（如"上次课结束于 N 天前，读到
     第 X 页附近"），并指示伙伴把上次课堂内容定位为「上次/之前」，不当作"今天/刚才"；开场自然承接
     上次的学习话题，但不要接续上次结尾的具体场景或画面。
- **涉及文件**：`chat-prompt.ts`、`prompt-builder.ts`
- **状态**: [x]

### A3. 概念学科提问从经验切入
- **现状**：无学科类型感知，伙伴可能一上来就抛出整门学科的结论。
- **参考**：3.1.0「概念性学科的提问更"够得着"——先从你的经验、直觉切入，再引向教材理论，
  而不是一上来就抛出整门学科的结论让你无从下手」。
- **实现思路**：`rules.ts` 新增规则段：当教材偏概念性学科（哲学/人文/心理等）时，先让学习者从
  自身经验、直觉出发，再逐步引向教材理论；数学/理科则保持从已知到未知的推导路径。
- **涉及文件**：`rules.ts`、`prompt-builder.ts`
- **状态**: [x]

### A4. 回应新想法 + 减少跳跃/重复提问/过简
- **现状**：无相关约束。
- **参考**：4.3.0「伙伴的教学节奏更加扎实，减少内容跳跃、重复提问和过于简单的问题，
  也能更好地回应学习中产生的新想法」。
- **实现思路**：`rules.ts` 新增规则段：学习者在讨论中提出新想法/问题/联想时优先回应它，
  不要机械回到原定问题；避免重复问已问过的问题；问题要有思考价值，避免过于简单；主题间过渡自然，不突兀跳跃。
- **涉及文件**：`rules.ts`、`prompt-builder.ts`
- **状态**: [x]

### A5. 课堂事实准确
- **现状**：`generate.ts` 的 LessonSummary prompt 未约束事实口径，可能把"计划要讲/讲解过"写成
  "学习者已经掌握"；伙伴刚提问、学习者还没回答就下课时，总结可能写成"全部完成"。
- **参考**：4.0.1「如果伙伴刚提出问题、你还没有回答就结束课堂，总结不会再写成"已经全部完成"」；
  4.5.0「伙伴会更谨慎地区分"提到过""真正问过""学习者回答过""准备以后讲"和"已经掌握"，
  减少把计划或讲解写成已经发生的学习经历」。
- **实现思路**：LessonSummary 生成 prompt 增加事实铁律段（见 A6 一并落地）。
- **涉及文件**：`generate.ts`
- **状态**: [x]

### A6. 长课堂总结照顾整段学习
- **现状**：总结生成只把全部对话拼成 transcript，长课堂里开头讨论的重要主题容易被后半段挤掉。
- **参考**：4.5.0「长课堂的课后笔记会照顾整段学习。开头讨论的重要主题不会再轻易被后半段挤掉；
  内容特别长时，也会兼顾课堂的起点、发展和结尾」。
- **实现思路**：
  1. LessonSummary prompt 增加要求：回顾时兼顾课堂起点、发展、结尾三段，开头重要主题不得被后半段挤掉。
  2. 长课堂（消息数超阈值）时，把 transcript 按时间分成 开头/中间/结尾 三段分别标注后注入，
     避免模型只记住结尾。
- **涉及文件**：`generate.ts`
- **状态**: [x]

---

## B. 闪卡复习

### B1. 闪卡「珍藏/全部」标签 + 批量操作
- **现状**：`FlashcardReviewView` 只有单一列表；无收藏标记；无批量删除/导出。
- **参考**：2.0.0「闪卡界面顶部从单"珍藏"变成"珍藏 / 全部"双标签页……支持"选择"模式：
  勾选若干卡片，批量删除或批量导出到 Anki」。
- **实现思路**：
  1. `useFlashcards` 新增 favorite 状态，存 `flashcard-favorites.json`（纳入 WebDAV 同步，同 SRS 模式）。
  2. 复习页加「珍藏/全部」标签、卡片收藏切换、勾选批量删除/导出。
  3. 批量删除需新增 IPC：按 artifactId + cardIndex 列表重写 flashcards artifact。
- **涉及文件**：`useFlashcards.ts`、`FlashcardReviewView.tsx`、`data.ts`、`flashcard-utils.ts`、`preload/index.ts`
- **状态**: [x]

### B2. Anki 导出范围选择
- **现状**：`buildAnkiImport` 只支持导出全部卡片。
- **参考**：4.5.0「闪卡可以按需要导出到 Anki。珍藏与全部闪卡分别导出各自范围；
  教材复习可以导出本教材的全部闪卡，课后复习可以只导出本课新卡」。
- **实现思路**：`buildAnkiImport` 加过滤参数（珍藏 / 全部 / 按教材 / 按课堂），UI 提供范围选择。
- **涉及文件**：`flashcard-utils.ts`、`FlashcardReviewView.tsx`
- **状态**: [x]

### B3. 课后即时复习本节新卡
- **现状**：下课后没有直接进入本节新卡复习的入口。
- **参考**：4.0.1「课后即时复习会展示本节全部新卡。只有新卡较少时，才会用以前的卡片补充复习」。
- **实现思路**：下课结果卡片加「复习本节新卡」按钮 → FlashcardReviewView 过滤到本 conversation 的卡；
  新卡 < 5 张时自动补充其他课堂的到期卡。
- **涉及文件**：`ClassroomView.tsx`、`FlashcardReviewView.tsx`、`useFlashcards.ts`
- **状态**: [x]

### B4. 闪卡数量随课堂内容增长
- **现状**：`generate.ts` Flashcards prompt 固定"3-5 张"，长课堂也不增加。
- **参考**：4.0.1「闪卡数量会跟随课堂内容自然增加。较长、内容较丰富的课堂会生成更多闪卡，不再过早封顶」。
- **实现思路**：按对话消息数/主题数动态生成数量（消息少 3-5 张，长课堂 5-10 张），
  并保留"只考对话里真讲过内容"约束。
- **涉及文件**：`generate.ts`
- **状态**: [x]

### B5. 闪卡质量规则
- **现状**：闪卡可能含数字陷阱题、答案与解析位置不一致、专业题丢前提等。
- **参考**：4.0.1「专业题目的表述更严谨……保留题目条件」「解析不再出现与正确答案位置不一致的字母；
  新卡不会凭空给学习者编名字」；4.3.0「闪卡更重视真正值得复习的知识，减少缺乏学习价值的数字陷阱题，
  答案说明也更加直截了当」。
- **实现思路**：Flashcards prompt 增加质量铁律段。
- **涉及文件**：`generate.ts`
- **状态**: [x]

---

## C. 日常学习体验

### C1. 课后「继续学习」按钮
- **现状**：下课后没有一键同教材开新课的入口。
- **参考**：1.0.7「课后页加"继续学习"按键：一键回到同教材开新课」。
- **实现思路**：下课结果卡片加「继续学习」（同教材、同伙伴开新课）；HistoryView 每条记录也可加。
- **涉及文件**：`ClassroomView.tsx`、`HistoryView.tsx`
- **状态**: [x]

### C2. 常用文本快捷模板
- **现状**：无常用文本片段插入。
- **参考**：1.0.7「账户 → 模板里设 9 条常用文字片段（每条 ≤ 120 字），通过按键点击或快捷键 1–9 在输入框插入」。
- **实现思路**：设置页新增「常用文本模板」管理（本地存储，≤ 9 条，每条 ≤ 120 字）；
  课堂输入区提供插入面板 + Alt+1..9 快捷键。
- **涉及文件**：`SettingsView.tsx`、`ClassroomView.tsx`、`preload/index.ts`（或 localStorage）
- **状态**: [x]

### C3. 全局字号四档
- **现状**：只有阅读器字号，界面字号固定。
- **参考**：1.0.7「字号四档调节：小/标准/大/超大」；3.2.0「四档字号差别拉得更开」。
- **实现思路**：App 根部 CSS 变量按档位切换界面字号；设置页选择；持久化到 localStorage 并即时生效。
  与阅读器字号独立。
- **涉及文件**：`App.tsx`、`main.css`、`SettingsView.tsx`
- **状态**: [x]

### C4. 划选公式复制还原 LaTeX（可选）
- **现状**：数学公式复制只有气泡右下角整条复制按钮。
- **参考**：1.0.9「支持鼠标划取复制公式——公式自动还原为 `$...$` / `$$...$$` 源代码」。
- **实现思路**：ChatMessage 公式渲染区拦截 copy 事件，把选中范围内的渲染公式还原为源码
  （通过 rehype 插件在公式元素上记录 `data-tex` 源码，copy 时替换回 `$...$` / `$$...$$`）。
- **涉及文件**：`ChatMessage.tsx`
- **状态**: [x]

---

## D. 数据安全

### D1. 删除前归档 / 回收站
- **现状**：删除课堂/世界/教材/伙伴/闪卡等直接物理删除，无恢复入口。
- **参考**：4.0.1「电脑版删除重要学习资料前会先留下归档……课堂、世界、教材、档案、伙伴、闪卡
  等内容，在移除前会先进入历史归档，方便以后找回」。
- **实现思路**：
  1. 新增 `archive-store.ts` + `archive/` 目录：删除前先把数据移到 `archive/{timestamp}_{id}/`。
  2. 新增 `archive:list` / `archive:restore` / `archive:purge` IPC。
  3. 在 `conversation:delete` / `textbook:delete` / `companion:delete` / `reading-note:delete`
     等 handler 接入归档逻辑；UI 提供「历史归档」入口。
- **涉及文件**：新增 `archive-store.ts`、`data.ts`、`companions.ts`、`preload/index.ts`、`HistoryView.tsx`/`TextbooksView.tsx`
- **状态**: [x]

### D2. 学习档案上锁（可选，P3）
- **现状**：无档案锁。
- **参考**：3.0.0「学习档案可以上锁——共用一台电脑时多一层安心」。
- **实现思路**：设置 PIN/密码（safeStorage 加密保存到 `config/profile-lock.enc`），
  启动时进入锁屏，验证通过后才显示内容；设置页可启用/关闭/立即锁定。
- **涉及文件**：`SettingsView.tsx`、`App.tsx`、`secure-key-store.ts`、新增 `ipc/lock.ts`
- **状态**: [x]

---

## 建议实施顺序

| 批次 | 项目 | 理由 |
|------|------|------|
| 第 1 批 | A1–A6 | 纯 prompt / 生成质量改进 — 改动小、见效快 |
| 第 2 批 | B3 + B4 + B5 | 课后新卡入口 + 闪卡生成质量 |
| 第 3 批 | B1 + B2 | 收藏/批量 + 导出范围 — 依赖 B4/B5 的卡片结构 |
| 第 4 批 | C1 + C2 + C3 | 日常体验 |
| 第 5 批 | D1 | 数据安全（删除前归档） |
| 第 6 批 | C4 / D2（可选） | 成本较高或与定位冲突，最后做 |

---

## v5.1 补充（2026-08-02）

> 继续逐条核对 changelog 后补充的一轮，全部完成。

### E1. PDF 导入跳过目录/序言 (1.0.9)
- `pdf-front-matter.ts` 纯函数启发式识别正文起始页（标题/版权/目录/序言/致谢页跳过，
  章节页/散文页视为正文，20% 上限防过度跳过）；`pdf-parser.ts` 改为按页提取后从正文起点拼接。
- 附带：`ParseResult` 增加 `bodyStartPage`。
- **状态**: [x]

### E2. 课堂记录 / 课后笔记 PDF 导出（含 KaTeX）(1.0.7/1.1.0/2.0.0)
- 新增 `pdf:export` IPC：渲染端用 react-dom/server + 与聊天气泡相同的 remark/rehype 管线
  把 markdown 转 HTML（KaTeX 已渲染），主进程开隐藏窗口内联 KaTeX CSS 后 `printToPDF`。
- HistoryView：每节课 + 每个课后产物加 📄 导出 PDF。
- **状态**: [x]

### E3. 课堂截图 (2.0.0/4.5.0)
- 新增 `screenshot:capture` IPC（`webContents.capturePage()` → PNG），课堂工具栏加 📷 截图按钮。
- **状态**: [x]

### E4. AI 代答 (3.2.0, Ctrl+Shift+A)
- 新增 `ai:compose-answer` IPC：用学习者第一人称起草一段回复草稿；课堂输入区「AI 代答」按钮 +
  Ctrl+Shift+A 快捷键（打字时也不误触发），草稿追加进输入框。
- **状态**: [x]

### E5. 删除入回收站提示 + HistoryView 继续学习
- 删除课程后提示「已移入历史归档可恢复」；已下课课程在历史页一键「继续学习」
  （同教材同伙伴开新课）。
- **状态**: [x]

### E6. 代码清理
- 移除 `FlashcardReviewView` 一个依赖不全的 auto-advance effect；确认 `as any` 已无残留
  （pdf-parser 的 polyfill 属必要）。
- **状态**: [x]

---

## v5.2 补充（2026-08-02）

### F1. TTS 循环播放 (3.2.0)
- `useTTS` 增加 `loop` 状态，`utter.onend` 在开启循环时自动重播；`TTSControlPanel` 增加 🔁 循环开关。
- **状态**: [x]

### F2. 语音输入触发词 (2.0.0)
- 新增 `voice-trigger.ts`（发送/清空两类触发短语，可配置）；
  课堂输入末尾说出触发短语即自动发送或清空（配合系统听写/第三方语音输入，免手对话）。
- 设置页「语音输入触发词」可配置两个短语；默认「发送」「清空」。
- **状态**: [x]

---

## v5.3 补充（2026-08-02）

### G1. EPUB 教材内搜索（Ctrl+F）
- 阅读器新增 🔍 搜索：全书按章统计命中、当前章 `<mark>` 高亮、▲/▼ 逐条跳转、
  命中章节列表一键跳转；Enter / Shift+Enter 下/上一个，Escape 关闭。
- 对应原版 3.1.0 课堂搜索能力在阅读场景的落地。
- **状态**: [x]

---

## v5.4 补充（2026-08-02）

### H1. PDF 阅读器增强
- `PdfReaderView` 新增：全书文本搜索（逐页提取并缓存，结果列表按页跳转）、
  当前页命中矩形高亮叠加、页码进度回写教材（WebDAV 同步）。
- **状态**: [x]

### H2. 课程复盘视图
- 新增 `ReviewView`（「历史 → 📋」进入）：把一节已下课课堂的产物整合为一页——
  课堂总结 / 自测题（逐级揭晓）/ 记忆卡片（内嵌复习，仅本课）/ 学习日记 / 学习进展 / 费曼知识蛋，
  顶部「继续学习」一键同教材同伙伴开新课。
- **状态**: [x]

---

## v5.5 补充（2026-08-02）—— 存储 / 同步 / 服务器文件管理

> 参考 anx-reader（SQLite 整库 + WebDAV，VACUUM INTO 快照 / 下载校验 / 备份恢复 / 远端版本保留）
> 与 readest（自建同步服务或 WebDAV 整库）的存储与同步管理思路，对照本项目的
> 文件系统存储 + WebDAV 文件级同步后的落地项。

### I1. 清理死代码
- 删除 `database-sync-manager.ts`（app.db 整库同步）及其测试——项目为文件系统存储，
  app.db 不存在、相关 IPC 从未接线，纯误导；同步 `sync-manager` 中 4 个死方法。
- **状态**: [x]

### I2. 拉取原子写入（防半截文件）
- pull 下载（文本与二进制）先写同目录临时文件再 `rename` 落位；
  传输中断/失败不会留下损坏的半截数据文件（此前是直接覆盖写目标路径）。
- **状态**: [x]

### I3. 冲突副本保护
- pull 覆盖前若 state 显示本地文件自上次同步后也有改动（双向修改），
  先把本地副本保留为 `{name}.conflict-{ts}` 兄弟文件，再以远端版本覆盖。
- **状态**: [x]

### I4. 远端回收站（anx-reader 的远端版本保留思路）
- push 移除远端文件前先 `MOVE` 到 `/sophia/.trash/{批次}/`（服务器不支持 MOVE 时回退 DELETE）；
  保留最近 3 批，每次 push 自动清理更旧批次；回收站对同步集不可见（不会被当 junk 删除、不会被下载）。
- 新增 `sync:list-trash` / `sync:empty-trash` IPC；推送确认文案改为「移入回收站可恢复」。
- **状态**: [x]

### I5. WebDavSyncView 增强（服务器文件管理）
- 新增「远端回收站」面板：刷新查看批次/文件数/体积，一键清空；
  推送/拉取结果报告 trashed / conflicts 计数。
- **状态**: [x]
