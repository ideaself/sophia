# Sophia — 环境变量模板（复制为 .env.local 使用）
#
# 渲染层 CSP 的 script-src 附加指令。留空 = 严格策略（script-src 'self'），
# 这是生产默认值。仅在使用 Vite 开发服务器（npm run dev）时需要注入
# 'unsafe-inline'（@vitejs/plugin-react 的 fast-refresh preamble 是内联脚本，
# 严格 CSP 会拦截并导致白屏）。
#
# 参考 .env.development / .env.production（两者已随仓库提供，通常无需修改）。
VITE_CSP_SCRIPT_SRC=
