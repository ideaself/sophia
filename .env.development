# Dev-only relaxation: @vitejs/plugin-react injects an inline preamble script
# for fast-refresh; the strict prod CSP (script-src 'self') blocks it and the
# app renders blank. .env.production keeps the empty value so prod stays strict.
VITE_CSP_SCRIPT_SRC='unsafe-inline'
