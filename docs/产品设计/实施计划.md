# 实施计划：第二层桌面版

日期：2026-07-06

## Overview

直接构建 Electron + React + Tailwind 桌面应用，实现单用户、本地优先、DeepSeek 驱动的 Sophia-like 核心学习闭环。跳过单页网页原型，不做社区、商城、订阅、多用户。

## Architecture Decisions

- DeepSeek-only：减少 provider 抽象复杂度，优先验证学习体验。
- Electron from start：用户明确要求直接第二层，并且需要本地文件系统和安全 Key 存储。
- Local-first storage：从第一版开始使用 appData 文件系统，避免后续从 localStorage 迁移。
- Single-user：不做登录、权限、多租户、云同步。
- Reuse all characters：9 个角色 Markdown 从 `reference/角色设定/candidates/` 进入本地候选角色池。

## Milestone 0: Project Bootstrap

### Task 1: Scaffold Electron + React + TypeScript project

**Description:** 建立应用骨架，包含 main、preload、renderer 三层和基础构建脚本。

**Acceptance criteria:**
- [ ] `npm run dev` 启动 Electron 窗口。
- [ ] renderer 显示基础 shell 页面。
- [ ] main/preload/renderer TypeScript 分层明确。

**Verification:**
- [ ] `npm run build` 成功。
- [ ] Electron 窗口打开，无控制台启动错误。

**Dependencies:** None

**Files likely touched:** `package.json`, `src/main/*`, `src/preload/*`, `src/renderer/*`

**Estimated scope:** Medium

### Task 2: Enforce Electron security baseline

**Description:** 配置 BrowserWindow 安全选项和最小 preload bridge。

**Acceptance criteria:**
- [ ] `contextIsolation: true`
- [ ] `nodeIntegration: false`
- [ ] `sandbox: true`
- [ ] renderer 无法直接访问 Node、fs、ipcRenderer。

**Verification:**
- [ ] 手动检查 renderer 中 `window.require` 不存在。
- [ ] preload 只暴露 `window.sophia`。

**Dependencies:** Task 1

**Files likely touched:** `src/main/window.ts`, `src/preload/index.ts`, `src/renderer/types/global.d.ts`

**Estimated scope:** Small

## Milestone 1: Local Data Foundation

### Task 3: Define shared schemas and IDs

**Description:** 用 TypeScript + Zod 定义 profile、world、companion、textbook、conversation、message、artifact 的输入/输出契约。

**Acceptance criteria:**
- [ ] 所有 IPC 输入都有 schema。
- [ ] 所有本地 JSON 文件有 schema。
- [ ] ID 类型和枚举集中定义。

**Verification:**
- [ ] 类型检查通过。
- [ ] schema 单元测试覆盖有效/无效样例。

**Dependencies:** Task 1

**Files likely touched:** `src/shared/schemas/*`, `src/shared/types/*`, `src/shared/errors.ts`

**Estimated scope:** Medium

### Task 4: Implement local appData layout initializer

**Description:** 启动时创建 `{appData}/Sophia-Local` 数据目录和 default profile/world。

**Acceptance criteria:**
- [ ] 首次启动自动创建目录。
- [ ] `story.md` 初始化为 `reference/world_preset.md` 内容。
- [ ] 9 个角色可被索引或复制到本地候选池。

**Verification:**
- [ ] 删除 appData 测试目录后重启可重新生成。
- [ ] 生成文件通过 Zod/读取校验。

**Dependencies:** Task 3

**Files likely touched:** `src/main/storage/app-data.ts`, `src/main/storage/initialize.ts`, `src/main/companions/reference-loader.ts`

**Estimated scope:** Medium

### Task 5: Implement secure DeepSeek key storage

**Description:** 用 Electron `safeStorage` 保存、读取、删除 DeepSeek API Key。

**Acceptance criteria:**
- [ ] Key 以加密文件保存。
- [ ] renderer 只能得到 `hasKey: boolean`，不能读取明文。
- [ ] 支持更新和删除 Key。

**Verification:**
- [ ] 本地 key 文件不是明文。
- [ ] 重启后 Key 状态仍可检测。

**Dependencies:** Task 2, Task 3

**Files likely touched:** `src/main/security/secure-key-store.ts`, `src/main/ipc/settings.ts`, `src/preload/index.ts`, `src/renderer/settings/*`

**Estimated scope:** Medium

### Checkpoint A: Foundation

- [ ] App starts.
- [ ] Secure bridge works.
- [ ] Local data directory initializes.
- [ ] API Key stored securely.
- [ ] Build and typecheck pass.

## Milestone 2: DeepSeek Chat Pipeline

### Task 6: Implement DeepSeek client in main process

**Description:** 封装 OpenAI-compatible DeepSeek client，支持 `deepseek-v4-pro` 和 `deepseek-v4-flash`。

**Acceptance criteria:**
- [ ] 非流式 chat 可调用。
- [ ] 错误码映射为统一 app error。
- [ ] API Key 不离开 main process。

**Verification:**
- [ ] 使用 mock client 测试 401/402/429/503 映射。
- [ ] 不需要真实 API Key 也能跑测试。

**Dependencies:** Task 5

**Files likely touched:** `src/main/llm/deepseek-client.ts`, `src/main/llm/errors.ts`, `src/main/ipc/chat.ts`

**Estimated scope:** Medium

### Task 7: Implement streaming chat IPC

**Description:** main process 调用 DeepSeek streaming，并通过 sessionId/channel 将 chunk 推给 renderer。

**Acceptance criteria:**
- [ ] renderer 可接收 token/chunk。
- [ ] 支持 cancel。
- [ ] 支持 error/end/usage 事件。

**Verification:**
- [ ] mock stream 测试正常、错误、中断。
- [ ] renderer 不直接建立外部 HTTP 连接。

**Dependencies:** Task 6

**Files likely touched:** `src/main/llm/stream-chat.ts`, `src/main/ipc/chat-stream.ts`, `src/preload/chat.ts`, `src/renderer/chat/useChatStream.ts`

**Estimated scope:** Medium

### Task 8: Implement prompt builder

**Description:** 根据角色、世界观、教材片段、学习者信息、历史尾巴和规则生成 system prompt。

**Acceptance criteria:**
- [ ] 9 个角色任一都能生成 prompt。
- [ ] Prompt 包含苏格拉底规则、旁白规则、下课规则、中文授课规则。
- [ ] 教材内容可插入且有长度限制。

**Verification:**
- [ ] snapshot tests 覆盖 Alice、Holmes、Sun Wukong。
- [ ] 超长教材片段被截断或窗口化。

**Dependencies:** Task 4, Task 3

**Files likely touched:** `src/main/prompt/prompt-builder.ts`, `src/main/prompt/rules.ts`, `src/main/prompt/token-budget.ts`, `tests/prompt-builder.test.ts`

**Estimated scope:** Medium

### Checkpoint B: LLM Pipeline

- [ ] Mock chat end-to-end works.
- [ ] Prompt builder output 可读且符合规则。
- [ ] Streaming IPC works without exposing Key.

## Milestone 3: Core Classroom UI

### Task 9: Build settings and first-run UI

**Description:** 用户可输入、更新、删除 DeepSeek API Key，并看到本地数据目录状态。

**Acceptance criteria:**
- [ ] 无 Key 时引导设置。
- [ ] 保存后进入主界面。
- [ ] 删除 Key 后无法发起聊天。

**Verification:**
- [ ] 手动走完整首次启动流程。

**Dependencies:** Task 5

**Files likely touched:** `src/renderer/settings/*`, `src/renderer/app-shell/*`

**Estimated scope:** Small

### Task 10: Build companion selector

**Description:** 展示全部 9 个角色，允许选择当前课堂角色。

**Acceptance criteria:**
- [ ] 9 个角色均可见。
- [ ] 选择后能影响 prompt builder。
- [ ] 至少显示名称、身份、简短性格摘要。

**Verification:**
- [ ] 手动切换 Alice、Holmes、Sun Wukong 后 prompt 使用对应角色。

**Dependencies:** Task 4, Task 8

**Files likely touched:** `src/renderer/companions/*`, `src/main/ipc/companions.ts`

**Estimated scope:** Small

### Task 11: Implement textbook import for Markdown/text

**Description:** 支持导入 `.md` / `.txt` 和手动粘贴文本，保存为本地 textbook。

**Acceptance criteria:**
- [ ] 可创建 textbook metadata。
- [ ] 内容保存为 `source.md`。
- [ ] 当前教材可被课堂选择。

**Verification:**
- [ ] 导入一个 Markdown 后可进入课堂并注入前 N 字。

**Dependencies:** Task 4, Task 3

**Files likely touched:** `src/main/textbooks/*`, `src/main/ipc/textbooks.ts`, `src/renderer/textbooks/*`

**Estimated scope:** Medium

### Task 12: Build streaming classroom chat UI

**Description:** 实现课堂聊天界面，显示用户消息、角色流式回复、Markdown/KaTeX/代码高亮。

**Acceptance criteria:**
- [ ] 用户可发送消息。
- [ ] assistant 回复流式显示。
- [ ] 消息写入本地 conversation。
- [ ] Markdown、公式、代码块渲染正常。

**Verification:**
- [ ] mock stream 和真实 DeepSeek 手动测试各一轮。

**Dependencies:** Task 7, Task 8, Task 10, Task 11

**Files likely touched:** `src/renderer/chat/*`, `src/main/conversations/*`, `src/main/ipc/conversations.ts`

**Estimated scope:** Large; if过大，拆为 persistence 与 UI 两个子任务。

### Checkpoint C: Classroom Loop

- [ ] 设置 Key → 选角色 → 选教材 → 发消息 → 流式回复 → 本地保存，全链路可用。
- [ ] 重启后 conversation 可恢复。

## Milestone 4: Lesson Artifacts

### Task 13: Implement end-class command and artifact generation

**Description:** 用户点击下课后，DeepSeek 生成 summary、flashcards、diary、progress、handoff tail。

**Acceptance criteria:**
- [ ] 下课只能由用户按钮触发。
- [ ] 生成结果保存到对应本地路径。
- [ ] 解析失败时保存 raw 输出并提示重试。

**Verification:**
- [ ] 完整课堂后点击下课，检查所有 artifact 文件存在。

**Dependencies:** Task 12

**Files likely touched:** `src/main/artifacts/*`, `src/main/ipc/artifacts.ts`, `src/renderer/artifacts/*`

**Estimated scope:** Medium

### Task 14: Implement lesson history and basic search

**Description:** 展示历史 conversation 和 artifact，支持关键词搜索消息内容。

**Acceptance criteria:**
- [ ] 历史列表按时间排序。
- [ ] 可打开旧课堂。
- [ ] 可搜索消息内容。

**Verification:**
- [ ] 多个 conversation 后搜索关键词返回正确结果。

**Dependencies:** Task 12, Task 13

**Files likely touched:** `src/main/search/*`, `src/renderer/history/*`

**Estimated scope:** Medium

### Checkpoint D: Complete Layer 2

- [ ] 完整闭环：上课、对话、下课、产物、历史、搜索。
- [ ] Build/typecheck/test pass。
- [ ] API Key 不泄露到 renderer。
- [ ] 数据目录可手动备份。

## Risks and Mitigations

| Risk | Impact | Mitigation |
|---|---|---|
| Electron 安全边界配置错误 | High | Task 2 单独验证；禁止 renderer 访问 Node |
| DeepSeek streaming IPC 复杂 | Medium | 先 mock stream，再接真实 API |
| Prompt 输出变成讲答案 | High | Prompt builder snapshot + 手动课堂 QA |
| 文件布局过度复杂 | Medium | 第一版保留层级但简化 message 存储 |
| 下课产物解析失败 | Medium | 保存 raw 输出，允许重试 |
| 角色内容公开复用存在 IP 风险 | Low for personal use | 当前仅个人自用；未来公开发布需重写角色 |

## Open Questions

- PDF/EPUB 是否进入第二层第一轮，还是在 Markdown/text 闭环完成后追加？当前计划后置。
- 是否需要语音朗读/TTS？当前计划后置。
- 是否需要学习统计图表？当前只做历史和基础搜索，图表后置。
